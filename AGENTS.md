# Load Lab contributor notes

- Keep anonymous execution pinned to the server-owned demo scenario.
- Never weaken target verification, egress allowlists, or budget caps for convenience.
- Preserve exact global allocation: shard-local stage targets must sum to the user request.
- Do not average regional percentiles; merge histogram counts.
- Use actual `@cloudflare/kumo` components when an equivalent exists.
- Run `npm run check` and build the generator image before deployment.
- Production resources default to the pgx account configured in `wrangler.jsonc`.
